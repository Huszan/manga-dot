import { ExpendableContentDirective } from './expendable-content.directive';

describe('ExpendableContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpendableContentDirective();
    expect(directive).toBeTruthy();
  });
});
